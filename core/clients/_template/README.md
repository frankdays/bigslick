# Client Context Pack
One folder per client. Copy this _template to clients/<client-name>/ and fill in each file.
Skills read the ACTIVE client's pack; switch clients with scripts/activate_client.sh.
Never put client specifics in skill files — only here.
