core/clients/*
!core/clients/_template/
!core/clients/hansel-ai/
core/clients/_active
.agents/
dist/
.env
*.key

# Build artifact — distributed as a GitHub Release asset, not in git history.
# A 6MB zip committed on every release would bloat the clone permanently.
bigslick.plugin
bigslick-*.zip
bigslick-*.dmg
__pycache__/
*.pyc
